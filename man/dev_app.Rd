% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dev.R
\name{dev_app}
\alias{dev_app}
\title{Run an app with hot reload}
\usage{
dev_app(file, poll = 1)
}
\arguments{
\item{file}{Path to an \code{.R} file that runs an rtui app.}

\item{poll}{Polling interval in seconds for file change detection
(used when waiting for changes after app exit). Default \code{1}.}
}
\value{
Called for its side effect (runs the app in a loop). Returns
\code{NULL} invisibly when interrupted.
}
\description{
Watches a \code{.R} file for changes and automatically re-runs it when saved.
The file is sourced directly in the current R session. When the app
exits (user quits or it crashes), the file's modification time is
checked and the app re-sources if the file changed.
}
\details{
\strong{Workflow:} edit your \code{.R} file in an editor, save, then quit the
running app (e.g. press \code{q}) — it restarts automatically with your
changes. If the app crashes, it waits for the next file save before
restarting.

Press \code{Ctrl+C} when the app is \strong{not} running (i.e. between restarts)
to stop the watcher.

\code{dev_app()} is designed for rapid development iteration. It:
\enumerate{
\item Sources and runs the given \code{.R} file
\item When the app exits, checks if the file was modified
\item If modified: re-sources immediately
\item If not modified: polls until the file changes, then re-sources
\item Continues until you press \code{Ctrl+C} between app runs
}

Because the file is sourced directly (not via a subprocess), the app
gets full terminal control and all keyboard/mouse input works normally.
}
\examples{
\dontrun{
# In a terminal:
dev_app("my_app.R")

# Edit my_app.R in your editor, save, quit the app — it restarts
}
}
