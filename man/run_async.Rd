% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/async.R
\name{run_async}
\alias{run_async}
\title{Run a function asynchronously in a background process}
\usage{
run_async(app, fn, name, args = list(), poll = 0.5)
}
\arguments{
\item{app}{An \code{RtuiApp} object (must be running).}

\item{fn}{A function to execute in the background. Must be
self-contained — it cannot access the parent session's variables.}

\item{name}{A unique name for this task (character string).}

\item{args}{A named list of arguments passed to \code{fn}. Default \code{list()}.}

\item{poll}{Polling interval in seconds. Default \code{0.5}.}
}
\value{
Invisible \code{app}.
}
\description{
Spawns an R background process via \code{callr::r_bg()} and polls for
completion.
When the task finishes, a \code{"task"} event is fired with:
\itemize{
\item \code{event$timer_id}: the task \code{name}
\item \code{event$value}: the return value of \code{fn} (or the error message on failure)
\item \code{event$widget_id}: \code{"__async_ok"} on success, \code{"__async_error"} on failure
}
}
\details{
Handle results with the \code{on_task} callback in \code{\link[=tui_app]{tui_app()}}.

The background process runs in a separate R session via the \code{callr}
package. The function \code{fn} must be self-contained: it cannot reference
objects from the calling environment. Pass any needed data through \code{args}.
}
\examples{
\dontrun{
quick_app(
  title = "Async Demo",
  layout = vstack(
    header(),
    static("Click to start a background task", id = "status"),
    button("Run Task", id = "run_btn"),
    footer()
  ),
  on_click = list(
    run_btn = function(event, state) {
      update(state$app, "status", content = "Working...")
      run_async(state$app, function() {
        Sys.sleep(3)
        42
      }, name = "my_task")
      state
    }
  ),
  on_task = function(event, state) {
    if (event$widget_id == "__async_ok") {
      update(state$app, "status",
             content = paste("Result:", event$value))
    } else {
      update(state$app, "status",
             content = paste("Error:", event$value))
    }
    state
  }
)
}
}
