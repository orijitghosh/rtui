% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forms.R
\name{tui_form}
\alias{tui_form}
\title{Create a form with named inputs and a submit button}
\usage{
tui_form(..., submit_label = "Submit", id = NULL, classes = NULL)
}
\arguments{
\item{...}{Named form field specs. Each should be an \code{rtui_spec} for an
input widget (e.g. \code{input()}, \code{select()}, \code{checkbox()}, \code{switch_input()}).
The name is used as both the widget id and the display label.}

\item{submit_label}{Label for the submit button (default \code{"Submit"}).}

\item{id}{Optional id for the enclosing container.}

\item{classes}{Optional CSS classes.}
}
\value{
An \code{rtui_spec} list representing the form layout.
}
\description{
Builds a vertical layout with labelled form fields and a submit button.
When the user presses the submit button, all current field values are
collected into a named list and passed to the \code{on_submit} callback.
}
\details{
The form auto-generates widget ids from the field names (spaces replaced
with underscores, lowercased). Use \code{on_click} with id \code{"__form_submit"}
to handle submission.

Field values are also collected and stored in state under \code{".form_values"}
each time a field changes. When submit is clicked the \code{on_click} handler
receives \code{event$value$form_data} with all current values.
}
\examples{
\dontrun{
quick_app(
  layout = vstack(
    header(),
    tui_form(
      Name = input(placeholder = "Your name"),
      Email = input(placeholder = "you@example.com", validators = "url"),
      Department = select(c("Engineering", "Marketing", "Sales")),
      Active = checkbox("Currently active", value = TRUE),
      id = "myform"
    ),
    footer(),
    id = "root"
  ),
  on_click = list(
    `__form_submit` = function(event, state) {
      vals <- state$get(".form_values", list())
      notify(state$app, paste("Name:", vals$name))
      state
    }
  )
)
}

}
