% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dialogs.R
\name{confirm}
\alias{confirm}
\title{Show a confirmation dialog}
\usage{
confirm(app, message, title = "Confirm", yes_label = "Yes", no_label = "No")
}
\arguments{
\item{app}{An \code{RtuiApp} object (or accessed via \code{state$app}).}

\item{message}{The question or message to display.}

\item{title}{Optional dialog title (default \code{"Confirm"}).}

\item{yes_label}{Label for the confirm button (default \code{"Yes"}).}

\item{no_label}{Label for the cancel button (default \code{"No"}).}
}
\value{
Invisible \code{app}.
}
\description{
Pushes a modal screen with a message and Yes/No buttons. The result
is dispatched as a \code{"screen_result"} event with \code{event$value} set to
\code{TRUE} (confirmed) or \code{FALSE} (cancelled).
}
\examples{
\dontrun{
quick_app(
  layout = vstack(
    button("Delete All", id = "del"),
    footer(),
    id = "root"
  ),
  on_click = list(
    del = function(event, state) {
      confirm(state$app, "Are you sure you want to delete everything?")
      state
    }
  ),
  on_screen_result = function(event, state) {
    if (isTRUE(event$value)) {
      notify(state$app, "Deleted!", severity = "warning")
    }
    state
  }
)
}

}
