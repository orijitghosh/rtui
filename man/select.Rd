% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widgets-input.R
\name{select}
\alias{select}
\title{Create a select dropdown widget}
\usage{
select(
  options,
  value = NULL,
  prompt = "Select...",
  id = NULL,
  classes = NULL,
  tooltip = NULL
)
}
\arguments{
\item{options}{Named character vector (values displayed, names used as keys)
or unnamed character vector (values used as both keys and display).}

\item{value}{Initial selected value (or NULL for no selection).}

\item{prompt}{Placeholder text when nothing is selected.}

\item{id}{Optional widget id.}

\item{classes}{Optional CSS classes (character vector).}

\item{tooltip}{Optional tooltip text shown on hover.}
}
\value{
An \code{rtui_spec} list.
}
\description{
Create a select dropdown widget
}
\examples{
\dontrun{
quick_app(
  layout = vstack(
    # Simple options
    select(c("Red", "Green", "Blue"), id = "color",
           prompt = "Pick a colour..."),
    # Named options (display => value)
    select(c("Small (S)" = "s", "Medium (M)" = "m", "Large (L)" = "l"),
           id = "size"),
    static("", id = "result"),
    id = "root"
  ),
  on_change = list(
    color = function(event, state) {
      update(state$app, "result",
             content = paste("Colour:", event$value))
      state
    }
  )
)
}

}
