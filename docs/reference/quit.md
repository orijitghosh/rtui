# Signal app termination from a callback

Signal app termination from a callback

## Usage

``` r
quit(result = NULL)
```

## Arguments

- result:

  Optional result to return from `app$run()`.

## Value

A sentinel object of class `"rtui_quit"`.
