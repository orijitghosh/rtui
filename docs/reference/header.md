# Create a header widget

Create a header widget

## Usage

``` r
header(show_clock = FALSE, id = NULL, classes = NULL)
```

## Arguments

- show_clock:

  Show a clock in the header.

- id:

  Optional widget id.

- classes:

  Optional CSS classes (character vector).

## Value

An `rtui_spec` list.
