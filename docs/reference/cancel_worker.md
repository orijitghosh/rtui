# Cancel a running worker

Cancel a running worker

## Usage

``` r
cancel_worker(app, name)
```

## Arguments

- app:

  An `RtuiApp` object.

- name:

  Worker name to cancel.

## Value

Invisible `app`.
