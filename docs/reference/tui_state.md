# Create a mutable TUI state object

Create a mutable TUI state object

## Usage

``` r
tui_state(initial = list())
```

## Arguments

- initial:

  A list of initial state values.

## Value

An R6 object of class `RtuiState`.
