# Create a scrollable container

Create a scrollable container

## Usage

``` r
scroll(..., id = NULL, classes = NULL)
```

## Arguments

- ...:

  Child widget specs.

- id:

  Optional widget id.

- classes:

  Optional CSS classes (character vector).

## Value

An `rtui_spec` list.
