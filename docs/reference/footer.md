# Create a footer widget

Create a footer widget

## Usage

``` r
footer(id = NULL, classes = NULL)
```

## Arguments

- id:

  Optional widget id.

- classes:

  Optional CSS classes (character vector).

## Value

An `rtui_spec` list.
