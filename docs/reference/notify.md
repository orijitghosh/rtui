# Show a notification

Show a notification

## Usage

``` r
notify(app, message, severity = c("info", "warning", "error"))
```

## Arguments

- app:

  An `RtuiApp` object.

- message:

  Notification message.

- severity:

  One of "info", "warning", "error".

## Value

Invisible `app`.
