# Wrap a function as a change-event handler

Wrap a function as a change-event handler

## Usage

``` r
event_change(handler)
```

## Arguments

- handler:

  A function taking `(event, state)`.

## Value

The handler with class `"rtui_change_handler"`.
