# Toggle dark mode

Switches the app between dark and light mode at runtime.

## Usage

``` r
dark_toggle(app, dark)
```

## Arguments

- app:

  An `RtuiApp` object.

- dark:

  Logical. If missing, toggles current mode.

## Value

Invisible `app`.
