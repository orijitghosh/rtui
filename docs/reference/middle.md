# Create a middle-aligned container (vertical centering)

Create a middle-aligned container (vertical centering)

## Usage

``` r
middle(..., id = NULL, classes = NULL)
```

## Arguments

- ...:

  Child widget specs.

- id:

  Optional widget id.

- classes:

  Optional CSS classes (character vector).

## Value

An `rtui_spec` list.
