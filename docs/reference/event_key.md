# Wrap a function as a key-event handler

Wrap a function as a key-event handler

## Usage

``` r
event_key(handler)
```

## Arguments

- handler:

  A function taking `(event, state)`.

## Value

The handler with class `"rtui_key_handler"`.
