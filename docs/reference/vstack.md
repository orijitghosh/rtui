# Create a vertical stack layout

Create a vertical stack layout

## Usage

``` r
vstack(..., id = NULL, classes = NULL)
```

## Arguments

- ...:

  Child widget specs.

- id:

  Optional widget id.

- classes:

  Optional CSS classes (character vector).

## Value

An `rtui_spec` list.
