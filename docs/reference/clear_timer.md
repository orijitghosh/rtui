# Cancel a running timer

Cancel a running timer

## Usage

``` r
clear_timer(app, name)
```

## Arguments

- app:

  An `RtuiApp` object.

- name:

  Timer name to cancel.

## Value

Invisible `app`.
