# Create a horizontal stack layout

Create a horizontal stack layout

## Usage

``` r
hstack(..., id = NULL, classes = NULL)
```

## Arguments

- ...:

  Child widget specs.

- id:

  Optional widget id.

- classes:

  Optional CSS classes (character vector).

## Value

An `rtui_spec` list.
