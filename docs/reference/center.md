# Create a center-aligned container

Create a center-aligned container

## Usage

``` r
center(..., id = NULL, classes = NULL)
```

## Arguments

- ...:

  Child widget specs.

- id:

  Optional widget id.

- classes:

  Optional CSS classes (character vector).

## Value

An `rtui_spec` list.
